version: "3.9"

services:
  app:
    restart: always
    environment:
      FLASK_ENV: production
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "1"
          memory: 512M
