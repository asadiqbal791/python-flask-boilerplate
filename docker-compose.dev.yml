version: "3.9"

services:
  app:
    build:
      context: .
      target: base
    command: python run.py
    environment:
      FLASK_ENV: development
    volumes:
      - .:/app
    ports:
      - "5000:5000"
      - "5678:5678"  # debugpy
